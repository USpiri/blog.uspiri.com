---
import { cn } from "@utils/cn";

interface Props {
  class?: string;
  as?: string;
}

const { class: className, as = "main" } = Astro.props;
const Element = as;
---

<Element class={cn("mx-auto w-full max-w-3xl px-5 sm:px-8", className)}>
  <slot />
</Element>
